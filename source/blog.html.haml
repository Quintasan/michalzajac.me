---
title: Blog
---

%section
  %h1 Recent posts
  - blog.articles.each do |article|
    %h2.title
      %a{href: article.url}= article.title
    %h3.date= article.date.strftime("%F")
    %p= excerpt(article)
